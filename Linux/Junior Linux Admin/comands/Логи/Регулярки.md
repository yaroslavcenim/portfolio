Как не путать

Просто запомни контекст:

· Если ты в терминале ищешь файлы — используй * (и ? для одного символа)
· Если ты в grep / locate -r — там регулярки, и там * без точки не работает, всегда надо .*